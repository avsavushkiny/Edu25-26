#include <iostream>
#include <set>
#include <vector>
using namespace std;

int main()
{
    int a = 2;
    vector<int> number_v = {2, 5, 87, 3, 2, 0};
    set<int> number = {2, 5, 87, 3, 4, 0};

    number_v.erase
    // number_v.insert(4+a);
    // number_v.erase(4);
   

    for (int i : number)
    {
        cout << i << endl;
    }

    return 0;
}